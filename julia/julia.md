= Julia =

== Syntaxes ==
() -> function
[] -> indexing
{} -> container for different sized objects
@  -> macro

== Comprehensions ==

[ i for i=1:5 ]
[ i + j for i=1:5, j=1:5 ]


== Some Useful Functions ==

typeof(object)

rand(dim)
svd(matrix) -> Singular Value Decomposition


imagesc(matrix) <from Winston?>
